import VehicleService from 'App/Models/VehicleService'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(VehicleService, ({ faker }) => {
  return {
    //
  }
}).build()
